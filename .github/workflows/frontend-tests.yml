name: Run Frontend Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  - uses: actions/checkout@v6
  - name: Use Node.js
    uses: actions/setup-node@v4
    with:
      node-version: "20.x"
  - name: Install dependencies
    run: npm install
  - run: npm test
